/**
 * Sample function.
 * @param  {Mixed} a Nondescript attribute.
 * @param  {Mixed} b Nondescript attribute.
 * @return {Boolean} Returns false.
 */
function test(a, b) {

	console.log(a, b);

	return false;

}
